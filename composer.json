{
  "name": "anton/json-items",
  "description": "Simple PHP library, allowing to read and write large lists of small items from/to JSON file (custom format), without loading whole file to memory.",
  "autoload": {
      "psr-4": {"JsonItems\\": "src"}
  }
}
